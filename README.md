# SubtitleExtender
Subtitle Extender is an Android app that extends only the end time of `.srt` subtitles by 0.5, 1, 1.5, or 2 seconds. It never changes subtitle start times and automatically prevents overlaps with the next subtitle. The original file remains unchanged, and a new edited subtitle file is created.
